upstreams:
  - name: user_service
    instances:
      - http://user-service:8001
      - http://user-service:8002

  - name: order_service
    instances:
      - http://order-service:8003
      - http://order-service:8004

  - name: product_service
    instances:
      - http://product-service:8005
      - http://product-service:8006

circuit_breaker:
  failure_threshold: 5
  recovery_timeout: 60s
  half_open_requests: 2

routes:
  - path: /api/v1/auth
    methods: ["get", "post", "patch", "delete"]
    strip_prefix: /api/v1
    auth_required: false
    check_role: false
    revoke_token: false
    roles: [admin, user]
    rate_limit:
      requests: 10
      window: 120s
      key_by: ip
    exceptions:
      - path: "/api/v1/auth/me"
        method: get
        auth_required: true
        rate_limit:
          requests: 10
          window: 120s
          key_by: user_email

      - path: "/api/v1/auth/refresh"
        method: post
        revoke_token: true
        rate_limit:
          requests: 10
          window: 120s
          key_by: ip

      - path: "/api/v1/auth/logout"
        method: post
        auth_required: true
        revoke_token: true
        rate_limit:
          requests: 10
          window: 120s
          key_by: user_email

      - path: "/api/v1/auth"
        method: delete
        auth_required: true
        revoke_token: true
        rate_limit:
          requests: 10
          window: 120s
          key_by: user_email

  - path: /api/v1/products
    methods: ["get", "post", "patch", "delete"]
    strip_prefix: /api/v1
    auth_required: true
    check_role: false
    revoke_token: false
    roles: [admin, user]
    rate_limit:
      requests: 50
      window: 60s
      key_by: user_email
    exceptions:
      - path: "/api/v1/products"
        method: post
        check_role: true
        roles: [admin]
        rate_limit:
          requests: 50
          window: 60s
          key_by: user_email

      - path: "/api/v1/products/{id}"
        method: patch
        check_role: true
        roles: [admin]
        rate_limit:
          requests: 50
          window: 60s
          key_by: user_email

      - path: "/api/v1/products/{id}"
        method: delete
        check_role: true
        roles: [admin]
        rate_limit:
          requests: 50
          window: 60s
          key_by: user_email

  - path: /api/v1/orders
    methods: ["get", "post", "delete"]
    strip_prefix: /api/v1
    auth_required: true
    check_role: false
    revoke_token: false
    roles: [admin, user]
    rate_limit:
      requests: 50
      window: 60s
      key_by: user_email

  - path: /api/v1/carts
    methods: ["get", "post", "patch", "delete"]
    strip_prefix: /api/v1
    auth_required: true
    check_role: false
    revoke_token: false
    roles: [admin, user]
    rate_limit:
      requests: 50
      window: 60s
      key_by: user_email
